# The pinned inputs for the embedded-Postgres bundle.
#
# Bump BUNDLE_REVISION when the build changes but neither upstream version does.
# The bundle tag is pg${PG_VERSION}-pgvector${PGVECTOR_VERSION}-r${BUNDLE_REVISION}

PG_VERSION=16.9.0
PGVECTOR_VERSION=0.8.0
BUNDLE_REVISION=1
